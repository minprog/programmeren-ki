grades:
    scratch:
        type: float
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    c:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    crypto:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    fifteen:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    python:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    simuleren:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    data:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    tentamen1:
        type: float
        subgrades:
            points: integer
        calculation: [1, ((points - 2) / 28.0 * 9 + 1).round(1)].max
        hide_calculated: false
    tentamen2:
        type: float
        subgrades:
            points: integer
        calculation: (points / 32.0 * 9 + 1).round(1)
        hide_calculated: false
    resit:
        type: float
        subgrades:
            points: integer
        calculation: (points / 24.0 * 9 + 1).round(1)

calculation:
    resit:
        resit: 40
        psets: 60
    normal:
        tentamens: 40
        psets: 60

psets:
    minimum: 5.5
    submits:
        c: 1
        crypto: 1
        fifteen: 1
        python: 1
        simuleren: 1
        data: 1

tentamens:
    minimum: 5.5
    submits:
        tentamen1: 1
        tentamen2: 1

resit:
    minimum: 5.5
    submits:
        resit: 1
